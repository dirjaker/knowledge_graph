# 知识图谱系统 — 技术设计文档

> **项目定位**：从文档自动构建知识图谱，支持可视化探索和自然语言问答的轻量级知识管理系统

---

## 一、项目背景与价值

### 1.1 为什么做

在企业知识管理中，大量非结构化文档（PDF、Word、Markdown）中的关键信息（实体、关系、事实）难以被结构化检索和推理。传统关键词搜索无法回答"张三和李四之间有什么联系"、"谁是阿里巴巴的CEO"这类关系型问题。

本项目实现了一个**端到端的知识图谱系统**，覆盖从文档解析到图谱构建、自然语言问答、可视化的完整链路，无需依赖外部图数据库（如 Neo4j），使用 SQLite + networkx 即可开箱即用。

### 1.2 面试话术

> "我做了一个知识图谱系统，主要解决从非结构化文档中自动抽取实体和关系、构建图谱并支持自然语言问答的问题。技术上用了 jieba 做分词+词性标注实现 NER，用模式匹配+共现分析做关系抽取，存储层用 SQLite 持久化 + networkx 内存图做双层存储，查询引擎基于正则意图识别支持实体查询、关系查询、路径查询、邻居查询四种模式。图算法层集成了度中心性、介数中心性、PageRank、社区发现等分析能力。整个系统用 FastAPI 提供 REST API，内嵌了 Canvas 可视化界面。"

---

## 二、系统架构

```
┌─────────────────────────────────────────────────────────┐
│                      用户界面层                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ 图谱可视化    │  │ 自然语言问答  │  │ 文档管理     │  │
│  │ (Canvas)     │  │ (Chat UI)    │  │ (上传/搜索)  │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└───────────────────────────┬─────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────┐
│                    API 服务层 (FastAPI)                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ 图谱 CRUD    │  │ 问答引擎     │  │ 文档解析     │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└───────────────────────────┬─────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────┐
│                    核心引擎层                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ 实体抽取      │  │ 关系抽取     │  │ 图谱推理     │  │
│  │ (NER)        │  │ (RE)         │  │ (多跳推理)   │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ 实体消歧      │  │ 关系分类     │  │ 图算法       │  │
│  │ (链接预测)    │  │ (分类器)     │  │ (中心性/社区)│  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└───────────────────────────┬─────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────┐
│                    存储层                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ SQLite       │  │ networkx     │  │ 文件存储     │  │
│  │ (持久化)      │  │ (内存图)     │  │ (文档)       │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 2.1 数据流

```
文档输入 → DocumentParser → 文本清洗 → EntityExtractor → RelationExtractor → GraphStore → 可视化/问答
   │                                                                         │
   └─ PDF/Word/MD/TXT ───────────────────────────────────────────────────────┘
```

---

## 三、核心模块设计

### 3.1 数据模型（models.py）

使用 Pydantic 定义核心数据结构，保证类型安全和序列化一致性：

```python
class EntityType(str, Enum):
    PERSON = "person"           # 人物
    ORGANIZATION = "organization"  # 组织
    LOCATION = "location"       # 地点
    CONCEPT = "concept"         # 概念
    TECHNOLOGY = "technology"   # 技术
    PRODUCT = "product"         # 产品

class Entity(BaseModel):
    id: Optional[str] = None
    name: str                           # 实体名称（唯一标识）
    entity_type: EntityType = EntityType.OTHER
    properties: dict = {}               # 额外属性
    aliases: list[str] = []             # 别名
    confidence: float = 1.0             # 置信度
    source_doc_ids: list[str] = []      # 来源文档

class Relation(BaseModel):
    source_entity: str                  # 源实体
    target_entity: str                  # 目标实体
    relation_type: RelationType         # 关系类型
    weight: float = 1.0                 # 权重（出现次数）
    confidence: float = 1.0             # 置信度
    evidence: str = ""                  # 证据文本

class Triple(BaseModel):
    """三元组 (主语, 谓语, 宾语)"""
    subject: str
    predicate: str
    obj: str
    confidence: float = 1.0
```

### 3.2 实体抽取模块（entity_extractor.py）

**四层抽取策略**，逐层补充，去重合并：

| 层级 | 方法 | 覆盖范围 | 置信度 |
|------|------|---------|--------|
| 1 | 正则匹配 | 日期、邮箱、URL、电话、金额 | 0.8 |
| 2 | jieba 词性标注 | 人名(nr)、地名(ns)、机构(nt) | 0.7-0.8 |
| 3 | 技术关键词词典 | 编程语言、框架、技术术语 | 0.9 |
| 4 | 已知实体词典 | 公司名、城市名 | 0.95 |

核心实现：

```python
class EntityExtractor:
    def extract(self, text: str, doc_id: str = "") -> list[Entity]:
        entities = []
        seen = set()

        # 1. 正则抽取（日期、邮箱、URL 等）
        for entity in self._extract_by_regex(text):
            if entity.name not in seen:
                entities.append(entity); seen.add(entity.name)

        # 2. jieba 分词抽取（人名、地名、机构名）
        if self.has_jieba:
            for word, flag in self.pseg.cut(text):
                if len(word) < 2: continue
                if flag == "nr":    # 人名
                    entity_type = EntityType.PERSON
                elif flag == "ns":  # 地名
                    entity_type = EntityType.LOCATION
                elif flag == "nt":  # 机构名
                    entity_type = EntityType.ORGANIZATION

        # 3. 技术关键词抽取
        for tech in TECH_KEYWORDS:  # Python, Docker, 机器学习, ...
            if re.search(re.escape(tech), text, re.IGNORECASE):
                entities.append(Entity(name=tech, entity_type=EntityType.TECHNOLOGY))

        # 4. 已知实体词典匹配
        for org in known_orgs:      # 阿里巴巴, 腾讯, ...
            if org in text:
                entities.append(Entity(name=org, entity_type=EntityType.ORGANIZATION))
```

### 3.3 关系抽取模块（relation_extractor.py）

**两种抽取策略并行**：

**策略一：模式匹配** — 基于预定义的语言模式模板：

```python
RELATION_PATTERNS = {
    RelationType.WORKS_AT: [
        r'({subject}).*?在.*?({target}).*?工作',
        r'({subject}).*?任职于.*?({target})',
    ],
    RelationType.CEO_OF: [
        r'({subject}).*?(?:是|担任|出任).*?({target}).*?(?:CEO|首席执行官)',
    ],
    RelationType.FOUNDED: [
        r'({subject}).*?(?:创立|创办|创建|成立).*?({target})',
    ],
    RelationType.LOCATED_IN: [
        r'({subject}).*?(?:位于|坐落在|在).*?({target})',
    ],
}
```

**策略二：共现分析** — 滑动窗口内实体两两建立关系，结合实体类型推断关系类型：

```python
def _extract_by_cooccurrence(self, sentences, entities):
    """同窗口内的实体 → 关系"""
    for i in range(len(sentences)):
        window_end = min(i + self.window_size, len(sentences))
        window_text = " ".join(sentences[i:window_end])
        window_entities = [e for e in entities if e.name in window_text]

        for j in range(len(window_entities)):
            for k in range(j + 1, len(window_entities)):
                e1, e2 = window_entities[j], window_entities[k]
                rel_type = self._infer_relation_type(e1, e2, window_text)
                # 人物+组织 → works_at, 组织+地点 → located_in

def _infer_relation_type(self, e1, e2, context):
    if e1.entity_type == PERSON and e2.entity_type == ORGANIZATION:
        return RelationType.WORKS_AT
    if e1.entity_type == ORGANIZATION and e2.entity_type == LOCATION:
        return RelationType.LOCATED_IN
    return RelationType.RELATED_TO
```

### 3.4 图谱存储模块（graph_store.py）

**双层存储架构**：SQLite 持久化 + networkx 内存图，启动时从 DB 加载到内存，写入时双写。

```python
class GraphStore:
    def __init__(self, db_path: str = "data/graph.db"):
        self.graph = nx.DiGraph()       # 内存图（查询用）
        self._init_db()                  # 建表
        self._load_graph()               # DB → 内存

    def add_entity(self, entity: Entity) -> str:
        # SQLite 持久化
        conn.execute("INSERT OR REPLACE INTO entities ...")
        # networkx 内存同步
        self.graph.add_node(entity.name, entity_type=entity.entity_type.value, ...)

    def add_relation(self, relation: Relation) -> str:
        # 重复关系 → 权重+1（而非重复插入）
        existing = conn.execute("SELECT weight FROM relations WHERE ...")
        if existing:
            conn.execute("UPDATE relations SET weight = weight + 1 ...")
        else:
            conn.execute("INSERT INTO relations ...")
        self.graph.add_edge(relation.source_entity, relation.target_entity, ...)
```

**数据库 Schema**：

```sql
CREATE TABLE entities (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL UNIQUE,
    entity_type TEXT DEFAULT 'other',
    properties TEXT DEFAULT '{}',       -- JSON
    aliases TEXT DEFAULT '[]',          -- JSON
    confidence REAL DEFAULT 1.0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE relations (
    id TEXT PRIMARY KEY,
    source_entity TEXT NOT NULL,
    target_entity TEXT NOT NULL,
    relation_type TEXT DEFAULT 'related_to',
    weight REAL DEFAULT 1.0,
    confidence REAL DEFAULT 1.0,
    evidence TEXT DEFAULT '',
    UNIQUE(source_entity, target_entity, relation_type)
);

CREATE INDEX idx_entities_name ON entities(name);
CREATE INDEX idx_relations_source ON relations(source_entity);
CREATE INDEX idx_relations_target ON relations(target_entity);
```

**图查询** — BFS 邻居发现 + 最短路径：

```python
def get_neighbors(self, entity_name: str, depth: int = 1) -> dict:
    """BFS 获取多跳邻居"""
    queue = [(entity_name, 0)]
    visited = {entity_name}
    while queue:
        current, d = queue.pop(0)
        if d >= depth: continue
        for neighbor in self.graph.successors(current):  # 出边
            if neighbor not in visited:
                visited.add(neighbor)
                queue.append((neighbor, d + 1))
        for neighbor in self.graph.predecessors(current):  # 入边
            ...

def find_path(self, source, target, max_depth=5):
    """最短路径，先查有向图，再查无向图"""
    try:
        return nx.shortest_path(self.graph, source, target)
    except nx.NetworkXNoPath:
        return nx.shortest_path(self.graph.to_undirected(), source, target)
```

### 3.5 查询引擎（query_engine.py）

**基于正则的意图识别**，无需 LLM 即可处理常见查询：

```python
class QueryEngine:
    def __init__(self, graph_store):
        self._patterns = {
            "entity_info": [
                r'(.+?)是(什么|谁|哪个)',
                r'告诉我关于(.+?)的信息',
            ],
            "relation": [
                r'(.+?)和(.+?)(什么关系|有什么关系)',
            ],
            "path": [
                r'(.+?)和(.+?)(之间|中间)(有什么联系|怎么联系)',
            ],
            "neighbor": [
                r'(.+?)(认识|知道|了解)(哪些|什么)(人|组织)',
                r'(.+?)的(同事|朋友|下属|上级)是(谁|哪些人)',
            ],
        }

    def query(self, request: QueryRequest) -> QueryResponse:
        intent, params = self._parse_intent(request.question)
        if intent == "entity_info":
            return self._query_entity_info(params)
        elif intent == "relation":
            return self._query_relation(params)
        elif intent == "path":
            return self._query_path(params, request.max_hops)
        elif intent == "neighbor":
            return self._query_neighbors(params, request.max_hops)
        else:
            return self._query_search(request.question, request.top_k)
```

支持的查询类型：

| 意图 | 示例问题 | 处理逻辑 |
|------|---------|---------|
| entity_info | "张三是什么职位？" | 查实体 → 返回属性+关系 |
| relation | "张三和阿里巴巴什么关系？" | 查直接关系 → 无则查路径 |
| path | "张三和李四之间有什么联系？" | BFS 最短路径 |
| neighbor | "张三认识哪些人？" | 多跳邻居展开 |
| search | 其他 | 模糊搜索实体 |

### 3.6 图算法模块（graph_algorithms.py）

基于 networkx 实现的图分析算法集合：

```python
class GraphAlgorithms:
    def degree_centrality(self, top_k=10):
        """度中心性 = 节点度数 / (n-1)"""
        return nx.degree_centrality(self.graph)

    def betweenness_centrality(self, top_k=10):
        """介数中心性 = 经过该节点的最短路径比例（桥梁节点）"""
        return nx.betweenness_centrality(self.graph)

    def pagerank(self, top_k=10):
        """PageRank 衡量节点重要性"""
        return nx.pagerank(self.graph)

    def detect_communities(self):
        """社区发现 — Louvain 算法，回退到 greedy_modularity"""
        try:
            from community import community_louvain
            partition = community_louvain.best_partition(undirected)
        except ImportError:
            communities = list(nx.community.greedy_modularity_communities(undirected))

    def get_important_nodes(self, top_k=10):
        """综合评分 = 0.4×度中心性 + 0.3×介数中心性 + 0.3×接近中心性"""
        scores[node] = 0.4 * degree + 0.3 * betweenness + 0.3 * closeness
```

### 3.7 文档解析模块（document_parser.py）

支持四种文档格式，统一输出 `Document` 对象：

```python
class DocumentParser:
    @staticmethod
    def _read_pdf(path: Path) -> str:
        import fitz  # pymupdf
        doc = fitz.open(str(path))
        return "".join(page.get_text() for page in doc)

    @staticmethod
    def _read_word(path: Path) -> str:
        from docx import Document as DocxDocument
        doc = DocxDocument(str(path))
        return "\n".join(p.text for p in doc.paragraphs)

    @staticmethod
    def _clean_text(text: str) -> str:
        text = text.replace("\r\n", "\n")
        text = re.sub(r"\n{3,}", "\n\n", text)  # 去除多余空行
        return text.strip()
```

### 3.8 API 服务层（api.py）

FastAPI 提供 RESTful API，内嵌完整的 Web UI（单文件 HTML+JS+CSS）：

```python
app = FastAPI(title="知识图谱系统", version="1.0.0")

# 核心组件初始化
store = GraphStore("data/graph.db")
query_engine = QueryEngine(store)
algorithms = GraphAlgorithms(store)

# 文档导入 → 自动抽取实体和关系
@app.post("/api/documents/ingest")
async def ingest_text(request: TextIngestRequest):
    doc = parser.parse_text(request.text, request.title)
    entities = entity_extractor.extract(request.text, doc.id)
    relations = relation_extractor.extract(request.text, entities, doc.id)
    for entity in entities: store.add_entity(entity)
    for relation in relations: store.add_relation(relation)
    return {"entities_extracted": len(entities), "relations_extracted": len(relations)}
```

---

## 四、技术亮点

### 4.1 SQLite + networkx 双层存储

- **SQLite** 负责持久化，支持索引、事务、并发安全
- **networkx** 负责内存图计算，图算法直接在内存执行
- 启动时 DB → 内存加载，写入时双写保证一致性
- 关系重复时自动累加权重，而非重复插入

### 4.2 多策略实体抽取

四层抽取策略逐层补充（正则 → jieba → 关键词 → 词典），每层有独立置信度，通过 `seen` 集合去重，高置信度来源优先。

### 4.3 模式匹配 + 共现分析的关系抽取

模式匹配覆盖明确语义（"张三在阿里巴巴工作"），共现分析兜底模糊关系（同窗口内的实体）。关系类型根据实体类型自动推断（人物+组织 → works_at）。

### 4.4 零依赖自然语言问答

不依赖 LLM，纯正则意图识别覆盖 4 种查询模式，响应时间 < 10ms。支持模糊匹配（实体名不精确时自动搜索最近结果）。

### 4.5 综合重要性评估

融合度中心性、介数中心性、接近中心性三种指标加权评分，识别图中最重要的"枢纽"节点。

### 4.6 单文件 Web UI

API 服务内嵌完整 HTML+CSS+JS 的 Canvas 可视化界面，支持力导向图布局、拖拽缩放、节点点击查看详情、实时问答交互。

---

## 五、面试常见问题

### Q1：为什么选择 SQLite + networkx 而不是 Neo4j？

**A**：项目定位是轻量级、零部署的知识图谱系统。SQLite 是 Python 内置的，无需额外安装数据库服务；networkx 是纯 Python 图计算库，图算法可以直接在内存中执行，开发调试方便。如果数据量达到百万级节点，可以平滑升级到 Neo4j，存储层接口设计上已经预留了切换能力。

### Q2：实体抽取的准确率如何保证？

**A**：采用四层抽取策略逐层补充，每一层有独立的置信度（已知词典 0.95 > 关键词 0.9 > jieba 0.8 > 共现 0.5）。通过 `seen` 集合去重，同名实体只保留最高置信度的结果。对于业务场景，可以通过扩展词典（`known_orgs`、`TECH_KEYWORDS`）快速提升准确率。

### Q3：关系抽取的模式匹配有什么局限性？

**A**：模式匹配依赖预定义的正则模板，无法覆盖所有语言表达。解决方案：（1）共现分析作为兜底策略；（2）预留了 LLM 抽取接口（`extract_from_llm`），后续可接入大模型提升召回率；（3）关系重复时权重累加，高频出现的关系置信度更高。

### Q4：图算法的实际应用场景是什么？

**A**：
- **度中心性**：找到连接最多的实体（核心人物/组织）
- **介数中心性**：找到"桥梁"节点（跨部门的关键人物）
- **PageRank**：找到图中最重要的实体
- **社区发现**：识别紧密关联的实体群组（如某个项目团队）
- **最短路径**：发现两个实体之间的隐含联系

### Q5：如何处理实体消歧问题？

**A**：当前版本通过精确匹配 + 模糊搜索实现基本消歧。查询时先精确匹配，失败后通过 `LIKE '%keyword%'` 模糊搜索取最相关结果。后续可加入向量相似度（embedding）实现语义消歧。

### Q6：系统性能瓶颈在哪？

**A**：当前瓶颈主要在：
1. **共现分析的 O(n²) 复杂度** — 实体数量多时两两配对开销大
2. **networkx 图算法** — 介数中心性复杂度 O(VE)，大图上较慢
3. **SQLite 并发** — 写入时锁表，并发写入受限

优化方向：批量写入、异步处理、升级到 Neo4j + Celery 异步任务队列。

### Q7：如果要接入 LLM 做抽取，架构上需要怎么改？

**A**：`EntityExtractor` 和 `RelationExtractor` 已经预留了 `extract_from_llm` 接口。只需要实现 prompt 模板，调用 LLM API，解析返回的 JSON 结果即可。由于抽取结果会经过置信度评估和去重，LLM 抽取可以和规则抽取共存互补。

---

## 六、项目数据

| 指标 | 数值 |
|------|------|
| 源码文件数 | 8 个 Python 文件 |
| 代码总行数 | ~3009 行 |
| 代码体积 | ~96 KB |
| Python 模块 | models, entity_extractor, relation_extractor, graph_store, query_engine, graph_algorithms, document_parser, api |
| 技术栈 | FastAPI, SQLite, networkx, jieba, Pydantic, pymupdf, python-docx |
| API 接口数 | 20+ 个 REST 端点 |
| 支持文档格式 | PDF, Markdown, Word, 纯文本 |
| 实体类型 | 8 种（person, organization, location, technology, concept, event, product, other） |
| 关系类型 | 11 种（works_at, located_in, ceo_of, founded, uses, created, related_to 等） |
| 图算法 | 5 种（度中心性, 介数中心性, 接近中心性, PageRank, 社区发现） |
| 查询模式 | 4 种（实体查询, 关系查询, 路径查询, 邻居查询） |
